不使用绑定变量，会导致硬解析高、CPU高，而且在AWR的top sql里不容易体现。同时，频繁解析sql，可能会过渡申请shared pool使得内存碎片化，进而导致ora-4031。
使用绑定变量，如果发生绑定变量窥视，而且数据倾斜，将导致执行计划改变，也可能出现性能问题。



硬解析太多-->内存碎片-->ora 4031
         -->latch争用


一个sql如果没绑定变量，而且执行次数很多，就容易导致硬解析过多。


有些情况不适合使用绑定变量，如t表中有status字段，99%是1,1%是0，此时若用绑定变量可能会出现错误的执行计划。



